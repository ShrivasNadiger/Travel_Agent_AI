{
    "version": 2,
    "buildCommand": "cd tourism-frontend-only && npm install && npm run build",
    "outputDirectory": "tourism-frontend-only/dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        }
    ]
}